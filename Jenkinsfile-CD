pipeline {
    agent any

    environment {
        HELM_RELEASE = "petclinic"
        HELM_CHART_REPO = "https://github.com/HCMUS-DevOps-Projects/project02-k8s.git"
        HELM_CHART_DIR = "helm-chart/charts"
        DEFAULT_NAMESPACE_PREFIX = "dev-review"
        GIT_REPO_CODE = "https://github.com/HCMUS-DevOps-Projects/project02.git"
        DOCKER_USER = "tlvu2004"
        KUBECONFIG = '/var/lib/jenkins/.kube/config'
    }

    stages {
        stage('Clone Helm Chart Repository') {
            steps {
                sh "rm -rf helm-chart"
                sh "git clone ${env.HELM_CHART_REPO} helm-chart"
            }
        }

        // stage('Clone Application Source Code Repository') {
        //     steps {
        //         dir('source-code') {
        //             deleteDir()
        //             sh "git clone ${env.GIT_REPO_CODE} ."
        //         }
        //     }
        // }

        stage('Generate values.yaml') {
            steps {
                def api_gateway_tag = getTag("api-gateway", params.api_gateway)
                def customers_service_tag = getTag("customers-service", params.customers_service)
                def discovery_server_tag = getTag("discovery-server", params.discovery_server)
                def vets_service_tag = getTag("vets-service", params.vets_service)
                def visits_service_tag = getTag("visits-service", params.visits_service)
                script {
                    def values = """
global:
  namespace: ${namespace}

services:
  api-gateway:
    image:
      tag: ${api_gateway_tag}
    ingress:
      host: petclinic-${namespace}.com

  config-server:
    image:
      tag: ${config_server_tag}

  customers-service:
    image:
      tag: ${customers_service_tag}

  discovery-server:
    image:
      tag: ${discovery_server_tag}

  vets-service:
    image:
      tag: ${vets_service_tag}

  visits-service:
    image:
      tag: ${visits_service_tag}
"""

                    writeFile file: "helm-chart/environments/dev-review/values-${namespace}.yaml", text: values
                    echo "Generated values-${namespace}.yaml"
                }
            }
        }
        stage('Deploy to Kubernetes') {           
            // when {
            //     expression { return params.DEPLOY_NAMESPACE != null }
            // }
            steps {
                echo "✅ DEPLOY_NAMESPACE = '${params.namespace}'"
                echo "✅ HELM_RELEASE = '${env.HELM_RELEASE}'"
                script {
                    def namespace = params.namespace.trim()
                    sh """
                        cd helm-chart
                        while kubectl get namespace ${namespace} >/dev/null 2>&1; do
                            echo "Waiting for namespace ${namespace} to be deleted..."
                            sleep 2
                        done
                        helm upgrade --install ${env.HELM_RELEASE}-${namespace} . \
                        -f environments/dev-review/values-${namespace}.yaml \
                        --namespace ${namespace} --create-namespace 
                    """
                }
            }
        }
    }

    post {
        always {
            echo "🧹 Cleanup..."
            sh "docker logout || true"
        }
    }
}

def getTag(serviceName, branch) {
    if (branch == "main") return "latest"
    def commit = sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
    return commit;
}
